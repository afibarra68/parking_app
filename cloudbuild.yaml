# Cloud Build configuration para compilar y desplegar en Cloud Run
steps:
  # Compilar la aplicación
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.cloud'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-services-repo/parking-backend:$SHORT_SHA'
      - '-t'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-services-repo/parking-backend:latest'
      - '.'

  # Push a Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-services-repo/parking-backend:$SHORT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-services-repo/parking-backend:latest'

  # Desplegar en Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'parking-backend'
      - '--image'
      - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-services-repo/parking-backend:$SHORT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'SPRING_PROFILES_ACTIVE=prod,DB_SOCKET=/cloudsql/${_CLOUD_SQL_INSTANCE},CLOUD_SQL_INSTANCE=${_CLOUD_SQL_INSTANCE}'
      - '--add-cloudsql-instances'
      - '${_CLOUD_SQL_INSTANCE}'
      - '--timeout'
      - '600s'
      - '--cpu'
      - '1'
      - '--memory'
      - '1Gi'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--set-secrets'
      - 'SPRING_DATASOURCE_PASSWORD=postgres_password:latest,SPRING_DATASOURCE_USERNAME=postgres_user:latest' 
      # Configuración VPC (opcional, descomentar si se usa VPC)
      # - '--vpc-connector'
      # - '${_VPC_CONNECTOR_NAME}'
      # - '--vpc-egress'
      # - 'private-ranges-only'

# Variables de sustitución (configurar en Cloud Build)
substitutions:
  _CLOUD_SQL_INSTANCE: 'parking-services-480307:us-central1:parkingprodgcloudb'
  _VPC_CONNECTOR_NAME: 'parking-vpc-connector'  # Opcional: nombre del VPC Connector

# Opciones de build
options:
  machineType: 'E2_MEDIUM'
  logging: CLOUD_LOGGING_ONLY

# Timeout
timeout: '1200s'

# Imágenes a almacenar
images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-services-repo/parking-backend:$SHORT_SHA'
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/parking-services-repo/parking-backend:latest'
