# Cloud Build configuration para compilar y desplegar en Cloud Run
steps:
  # Compilar la aplicación
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.cloud'
      - '-t'
      - 'gcr.io/$PROJECT_ID/parking-backend:$SHORT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/parking-backend:latest'
      - '.'

  # Push a Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/parking-backend:$SHORT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/parking-backend:latest'

  # Desplegar en Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'parking-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/parking-backend:$SHORT_SHA'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'SPRING_PROFILES_ACTIVE=prod,DB_SOCKET=/cloudsql/${_CLOUD_SQL_INSTANCE},CLOUD_SQL_INSTANCE=${_CLOUD_SQL_INSTANCE},PORT=8080'
      - '--add-cloudsql-instances'
      - '${_CLOUD_SQL_INSTANCE}'
      - '--timeout'
      - '300s'
      - '--cpu'
      - '1'
      - '--memory'
      - '512Mi'
      - '--min-instances'
      - '0'
      - '--max-instances'
      - '10'
      - '--set-secrets'
      - 'SPRING_DATASOURCE_URL=parking-app-db-url:latest,SPRING_DATASOURCE_USERNAME=parking-app-db-username:latest,SPRING_DATASOURCE_PASSWORD=parking-app-db-password:latest,SPRING_JWT_SECRET=parking-app-jwt-secret:latest'
      # Configuración VPC (opcional, descomentar si se usa VPC)
      # - '--vpc-connector'
      # - '${_VPC_CONNECTOR_NAME}'
      # - '--vpc-egress'
      # - 'private-ranges-only'

# Variables de sustitución (configurar en Cloud Build)
substitutions:
  _CLOUD_SQL_INSTANCE: 'testauth20-394320:us-central1:parkingprodgcloud'
  _VPC_CONNECTOR_NAME: 'parking-vpc-connector'  # Opcional: nombre del VPC Connector

# Opciones de build
options:
  machineType: 'N1_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

# Timeout
timeout: '1200s'

# Imágenes a almacenar
images:
  - 'gcr.io/$PROJECT_ID/parking-backend:$SHORT_SHA'
  - 'gcr.io/$PROJECT_ID/parking-backend:latest'

