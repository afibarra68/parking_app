# Configuración para tests
spring:
  application:
    name: users_ms_test
    jwtSecret: "4A404E635266556A586E3272357538782F413F4428472D4B6150645367566B59"
    jwtExpirationInMs: "100000000"
  sql:
    init:
      mode: never
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        jdbc.lob.non_contextual_creation: true
    open-in-view: false
  flyway:
    enabled: false

# Deshabilitar Secret Manager durante tests
gcp:
  secret-manager:
    enabled: false

server:
  port: 0  # Puerto aleatorio para tests

logging:
  level:
    root: WARN
    com.webstore.usersMs: INFO
    org.springframework: WARN
    org.hibernate: WARN

